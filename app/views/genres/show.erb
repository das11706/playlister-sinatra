<h2><%=@genre.name%></h2>

<h4>Artist Names</h4>
<ul>
 <% @genre.songs.each do |song| %>
    <li><a href="/artists/<%= song.artist.slug %>"><%= song.artist.name %></a></li>
    <% end %>
</ul>

<h4>

<h4>Song Names</h4>
<ul>
  <% @genre.songs.each do |song| %>
    <li><%= song.name %></li>
  <% end %>
</ul>


